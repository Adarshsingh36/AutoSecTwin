# Digital Twin Generator dependencies.
# Merge into the project's existing requirements -- versions are minimums
# consistent with the rest of AutoSecTwin's stated stack (Python 3.11+,
# FastAPI, SQLAlchemy, Pydantic v2).

fastapi>=0.110
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
alembic>=1.13
docker>=7.0
structlog>=24.1

# Testing
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20
httpx>=0.27
